const ROLES = ["student", "alumni", "faculty", "mentor", "admin"];
const PUBLIC_REGISTER_ROLES = ["student", "alumni"];
const SCOPED_KEYS = ["school", "major", "class_section", "cohort"];

module.exports = {
  ROLES,
  PUBLIC_REGISTER_ROLES,
  SCOPED_KEYS
};
